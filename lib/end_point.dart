const String errorMessagePriority =
    "Todo validation failed: priority: `Medium` is not a valid enum value for path `priority`.";
const String errorMessageDesc =
    "Todo validation failed: desc: Path `desc` is required.";
const errorMessageTitle = "Todo validation failed: title: Path `title` is required.";